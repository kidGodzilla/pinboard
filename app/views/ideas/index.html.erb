<br/>

<% @ideas.in_groups_of(3) do |group| %>
    <div class="row">
      <% group.compact.each do |idea| %>
          <div class="col-md-4">
            <%= link_to idea do %>
                <%= image_tag idea.picture_url, width: '100%' if idea.picture.present?%>
                <h4 style="color: #2196f3;"><%= idea.name %></h4>
            <% end %>
            <%= idea.description %>
          </div>
      <% end %>
    </div>
<% end %>