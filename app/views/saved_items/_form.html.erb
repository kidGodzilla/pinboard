<%= form_for(@saved_item) do |f| %>
  <% if @saved_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@saved_item.errors.count, "error") %> prohibited this saved_item from being saved:</h2>

      <ul>
      <% @saved_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :item_id %><br>
    <%= f.text_field :item_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
